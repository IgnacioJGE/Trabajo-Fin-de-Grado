'use client';

import React from 'react'
import { useState } from 'react';

type pregunta={
    texto:string;
}
const conversacion = (pregunta:pregunta) => {
    const [textos, setTextos] = useState([]); 
    const [inputTexto, setInputTexto] = useState(""); 

  return (
    
  )
}

export default conversacion